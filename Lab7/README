a) Team Members:
Arun Ganesh, Nikhil Nagarajan, Garrett Ravatt

b) Compilation/Runtime Instructions:
Setting up Environmental Variables

$ export CLASSPATH=$CLASSPATH:mysql-connector-java-8.0.16.jar:.
$ export HP_JDBC_URL=jdbc:mysql://db.labthreesixfive.com/your_username_here?autoReconnect=true\&useSSL=false
$ export HP_JDBC_USER=<username>
$ export HP_JDBC_PW=<pwd>
$ cd lab7_JDBC
$ javac InnReservations.java
$ java InnReservations

WINDOWS:
- download jar file
- add system environmental variables to PC
    HP_JDBC_PW
    - lab365password
    HP_JDBC_URL
    - jdbc:mysql://db.labthreesixfive.com/calpolyusername?autoReconnect=true&useSSL=false
    HP_JDBC_USER
    - calpolyusername
 - after setting up go to command prompt:
   a) javac InnReservations.java
  
   b) java -cp mysql-connector-java-8.0.16.jar;. InnReservations

 
c) Known bugs and deficiences:
- We have only run this program on Windows and not sure how it will run on Linux.
- FR2 has some bugs
